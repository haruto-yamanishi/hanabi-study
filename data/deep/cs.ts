import { deep, decision as q } from './types';
export const csDeep = [
deep('cs-programming','入力契約と副作用のない関数',
'関数の契約は、許す入力、返す値、失敗時の扱いからなる。型が数値でもNaN・Infinity・範囲外は入り得る。境界で検査し、正常なゼロと異常を同じ戻り値で表さない。\n物理量の計算とモータへの書込みを分けると、計算だけをテストできる。副作用のない関数は同じ入力に同じ出力を返す。状態が必要な部分は別に保持し、呼ぶ順番への依存を明示する。',
'擬似コード：toMeters(counts, countsPerRev, ratio, circumference)。先に各値が有限でcountsPerRev>0、ratio>0を検査し、counts/countsPerRev/ratio×circumferenceを返す。不正なratio=0を0 mとして返すと、原点にいるという誤った判断を誘発する。',
[['異常値を0として返すと何が起きる？','本当のゼロと区別できず、原点や停止と誤認する。エラー型・状態フラグなどで区別する。'],['計算とハードウェア書込みを分ける利点は？','入力だけを与えて再現テストでき、試験中に実機を動かさず計算の正しさを確認できる。']],
[q('ratio=0が入力された変換関数の適切な応答は？',['異常を明示して出力側へ伝える','原点とみなし0 mを返す','無限大をそのまま制御に渡す'],0,'分母0は入力契約違反。正常な位置と区別し、上位が安全な動作を選べるようにする。'),q('同じ入力で値が変わる純粋な計算関数。確認すべきものは？',['変数名の長さ','時刻・乱数・共有状態への隠れた依存','コメントの行数'],1,'外部状態に依存すると再現性が失われる。必要な値を引数へ渡す。')],
'単位変換を純粋関数として実装し、ゼロ・負カウント・ratio=0・NaNをテストする。',
'正負の位置は有効に扱い、ゼロ除算と非有限値は明示的に失敗させる。計算テストからモータを書き換えない構造にする。'),
deep('cs-debug','最小再現と反証できる仮説',
'最小再現は症状を保ったまま不要な処理を削った実験。原因だと思う要素を除いても症状が残るなら、その仮説を見直せる。再現の入力、版、環境、時刻を固定する。\n「通信が悪い」のような曖昧な仮説を、「測定年齢が100 msを超えたときだけ出力が止まる」のような観測可能な形へ変える。正常に動いた1回より、仮説の予測と一致する比較実験の方が原因を絞りやすい。',
'カメラ接続中だけ周期超過する。画像処理を外し受信だけ残す→超過なし、保存だけ戻す→超過あり、保存先をメモリへ変える→超過なし。同期ファイルI/Oが候補だが、ディスク負荷を変えた再試験で確かめる。',
[['反証できる仮説とは？','間違っていればどの観測が得られるかを事前に述べられる仮説。'],['複数箇所を同時に直すと原因調査で困る理由は？','どの変更が効いたか分からず、根本原因と偶然の改善を区別しにくい。']],
[q('AとBを同時に変更して症状が消えた。次に必要なのは？',['原因はAと断定','元の条件を再現してA/Bの効果を分ける','正常ログを削除'],1,'一つずつ比較し因果候補を絞る。'),q('ログが不足して再現できない障害への第一歩は？',['無作為なdelayを入れる','全コードを置き換える','期待値・条件・境界の観測を追加する'],2,'観測できない仮説を試すために必要な記録を設計する。')],
'遅延バグの最小再現コードと、原因を否定できる実験を2つ作る。',
'入力、ソフト版、タイミングを固定し、予測と結果を並べる。バグが再現しないだけの場合は原因特定済みと書かない。'),
deep('cs-data','リングバッファと起動直後の境界',
'リングバッファは固定容量の配列へ循環して書き、古い値を置換する。書込み位置と有効件数を別に持つ。配列容量とサンプル数を混同すると、起動直後に未使用の0を平均へ含めてしまう。\n移動平均をO(1)で更新するには、満杯なら追い出す値を合計から引き、新値を足す。満杯になる前は有効件数で割る。タイムスタンプも持ち、固定件数が固定時間とは限らないことに注意する。',
'容量4、データ[8,12]だけの起動直後なら平均は10で、配列の残り0を含めた5ではない。次に16,20,24を入れると保持[12,16,20,24]で平均18。論理順序と物理添字は一致しない場合がある。',
[['リングバッファで容量と有効件数を別に持つ理由は？','まだ埋まっていない領域を実データと誤認せず、平均や履歴取得を正しく行うため。'],['O(1)の移動平均は何を保持する？','現在の合計、有効件数、置換位置。古い値を引き新しい値を足す。']],
[q('容量10に2件しかない時の平均の分母は？',['2','10','0'],0,'実測した有効件数で割る。'),q('固定件数バッファで周期が揺れたとき、保存時間は？',['必ず一定','自動で2倍','サンプル時刻に応じて変わる'],2,'件数一定でも時間幅は変わる。時刻を保存して判断する。')],
'容量4の履歴を実装し、空・1件・満杯・3周後の内容と平均を検査する。',
'空の平均を正常な0と混同しない。有効件数は4を超えず、最新4件の順序と合計を手計算で照合する。'),
deep('cs-algorithms','探索の正しさを不変条件で説明する',
'不変条件は反復の開始・終了で保たれる性質。二分探索なら「答えが存在するなら探索区間内にある」と定め、区間を縮めても保たれることを示す。開始と終了条件を含めて正しさを説明する。\n高速でも端点で無限ループしたり、見つからない場合に不正添字を返せば使えない。空配列、1要素、最初、最後、存在しない値をテストし、最悪実行回数も記録する。',
'半開区間[lo,hi)でlower_boundを探す。mid=(lo+hi)//2、a[mid]<targetならlo=mid+1、そうでなければhi=mid。lo<hiの間繰り返し、終了時loは初めてtarget以上になる位置。すべて小さければ配列長になる。',
[['半開区間[lo,hi)の意味は？','loを含みhiを含まない。長さはhi−loで、lo=hiなら空区間。'],['二分探索の不変条件と進行性を別々に確かめる理由は？','答えを捨てないことと、区間が縮まり終了することは別の性質だから。']],
[q('整列配列[2,4,6]でlower_bound(7)を返す仕様。正しい値は？',['添字2','配列長3','必ず例外'],1,'target以上の要素がないので末尾の次を返す仕様。'),q('2要素でlo=midと更新し区間が縮まらない。必要な修正は？',['入力を増やす','待機を挟む','区間の定義に合う更新と進行性の確認'],2,'整数丸めと境界更新を整合させる。')],
'lower_boundを実装し、線形探索の参照実装と多数の小配列で結果を比較する。',
'重複値・空配列・範囲外を含める。不変条件、毎回区間が縮む根拠、返す添字の意味を説明する。'),
deep('cs-git','回帰原因をbisectで特定する',
'既知の正常commitと異常commitの間を二分して、症状が初めて現れた変更を探せる。判定手順を自動化できると再現性が上がるが、テストが不安定なら誤ったcommitを犯人にする。\n作業中の変更を保護し、確認した版と判定結果を記録する。ビルド不能や外部環境の不足を「バグなし」と扱わず判定不能として区別する。原因commitの特定後も変更の意図を読み、修正と回帰テストを作る。',
'8個の候補をテストで半分ずつ絞ると、約3回の縮小で候補1個になる。ただし途中版がビルド不能ならskipが必要。バグ判定を「プロセスが起動した」だけにすると、速度の単位ミスなどを見逃す。',
[['bisectが役立つ条件は？','正常と異常の版が分かり、同じ症状を安定して判定できること。'],['ビルド不能の版をgoodにしてはいけない理由は？','対象の振る舞いを確認できておらず、バグがない証拠にならないため。']],
[q('二分調査の中間版がビルド不能。判定は？',['good','判定不能として扱う','症状の原因と即断'],1,'対象バグの有無とは分けて扱う。'),q('単位変更commitが原因だった。必要な修正は？',['変更意図と呼出側契約を調べ回帰テストを追加','履歴をすべて削除','原因版だけ名前変更'],0,'変更が悪いというだけでなく、契約がどこで破れたかを直す。')],
'練習リポジトリで途中に単位バグを入れ、同じ入力の自動判定で発生版を探す。',
'作業前の状態を保存し、good/badの判断基準を示す。原因版だけでなく、修正後に再発を検出するテストも提出する。'),
deep('cs-state-machine','不変条件と遷移優先順位',
'状態表には正常イベントだけでなく、同じ周期に複数イベントが来る場合の優先順位を定める。停止、異常、通常操作のどれを先に処理するかを明示し、実装のif順に偶然依存させない。\n不変条件として「無効化中は駆動出力0」「原点未確定なら位置目標を受け付けない」などを置く。すべての状態・遷移で条件が保たれるか検査すると、正常経路だけのテストより漏れを見つけやすい。',
'取込完了と停止要求が同時に来た場合、停止が優先なら出力0で停止状態へ進む。完了処理で次commandを起動してから停止すると、1周期だけ意図しない出力が出る可能性がある。',
[['状態遷移の優先順位が必要になるのはいつ？','停止と完了など複数イベントが同時に成立したとき。'],['不変条件と特定の遷移テストは何が違う？','不変条件はすべての状態・操作列で常に守る性質、遷移テストは具体的な条件の例。']],
[q('停止と取込完了が同時に来る仕様が未定。先にすることは？',['実装順へ任せる','完了を常に優先する','要求に基づき優先順位を定義する'],2,'同時イベントの挙動を仕様にしてテストする。'),q('無効化後に1周期だけ出力が残る。検証すべきものは？',['全状態からの停止経路と出力更新順','メニューの文字','最大速度だけ'],0,'例外状態や終了処理も含めて停止不変条件を確認する。')],
'停止・完了・タイムアウトを同時発生させるイベント列を作り、出力が仕様を破らないか検証する。',
'優先順位表と全状態からの停止試験を提出する。異常復帰は原因解消と再始動意図を区別する。'),
deep('cs-testing','性質を使ったテストと独立した期待値',
'具体的な値を列挙するだけでなく、入力範囲全体で保つ性質をテストできる。clampなら出力は上下限内、すでに範囲内なら入力と等しい、2回適用しても同じ値という性質がある。\n期待値を実装と同じ式で計算すると、同じ誤りを共有する。手計算、別アルゴリズム、保存則、逆変換など独立な根拠を使う。乱数を使う場合はseedと失敗入力を保存して再現できるようにする。',
'回転変換では長さが保存される、逆変換で元に戻る、90°を4回で一周する性質を調べる。ただし両方で同じ誤った角度規約を使うと往復だけは通るので、既知の方向テストも必要。',
[['往復変換テストだけでは不十分な場合は？','正変換と逆変換が同じ誤った規約を共有すると、往復は成功しても外部との対応が間違う。'],['ランダムテストの失敗で保存するものは？','乱数seed、具体的な失敗入力、ソフト版と期待する性質。']],
[q('回転と逆回転のテストは通るがfield方向が違う。追加するのは？',['既知の90°方向と外部基準との比較','同じ往復試験だけ100倍','期待値を実装からコピー'],0,'独立な基準で座標規約も確認する。'),q('乱数テストが1回だけ失敗した。最も有効な記録は？',['実行した曜日だけ','seedと失敗入力と版','成功率だけ'],1,'そのケースを決定的に再現し回帰テストへ残す。')],
'clampと回転関数へ性質テストを作り、故意に符号バグを入れて検出する。',
'既知値テストと性質テストの両方を用意する。失敗を再実行できる形で保存し、実装と期待値が同じ式だけにならないようにする。'),
deep('cs-networks','メッセージの重複・欠落・順序',
'通信の到達と、内容が現在の判断に使えることは別。メッセージへ連番、観測時刻、種類、単位を付け、重複・順不同・欠落を検出する。受信時刻だけではいつ測ったか分からない。\n再送で同じ命令が2回届く場合、「位置を0.1 m増やす」は2回分動いてしまう。命令IDで重複排除するか、「目標位置を0.5 mへ設定」のように繰り返しても結果が同じ操作を使う。',
'連番100,102,101で観測が届いた。102が最も新しい観測とは限らず、送信側の時刻規約を確認する。順序が確定している場合、後着101で現在値を巻き戻さず、必要なら時刻付き履歴へ入れる。',
[['冪等な指令の例と利点は？','目標位置を固定値へ設定する指令。同じ命令が再送されても余分に移動しない。'],['受信時刻と撮影時刻を別に持つ理由は？','ネットワークや処理の遅れがあるため。データの古さと対応する状態を判定する。']],
[q('同じ増分移動命令が再送される。対策は？',['毎回そのまま加算','命令IDの重複排除または冪等な目標指令','連番を削除'],1,'再送が余分な動作へ変わらないようにする。'),q('古い観測が後から届いた。どう扱う？',['無条件に現在値を上書き','受信した順に最新扱い','時刻と順序規約で鮮度を判定'],2,'到着順と観測順は別。推定器の履歴処理に合う形で扱う。')],
'3件の観測を順序変更・重複・欠落させ、受信側の状態更新をテストする。',
'命令と観測を区別し、重複排除と古いデータの扱いを明記する。通信断後の復帰でも古い命令を実行しない。'),
deep('cs-architecture','共有状態のスナップショット',
'別スレッドや非同期処理が同じ状態を更新すると、読み取り時に異なる時刻の値を組み合わせることがある。位置だけ新しく速度だけ古い組は、一見各値が正常でも物理的に不整合になる。\n関連する値と時刻を一つの不変オブジェクトとして公開し、周期開始時に参照を1回取得する方法がある。言語と実行環境に合う同期方法を使い、長いロック待ちを制御周期へ持ち込まない。',
'producerがposition、velocity、timestampを順に更新し、consumerが途中で読むと組が崩れる。完成したSampleをまとめて受け渡せば、一つの観測時刻に対応する値を扱える。',
[['各値が正しくても組合せが不正になる例は？','更新途中に新しい位置と古い速度を読む場合。同じ時刻の測定ではない。'],['スナップショットへ時刻を含める利点は？','値同士の対応と鮮度を判断し、古いデータを検出できる。']],
[q('位置と速度が別々に更新され不整合。改善案は？',['各変数をさらに細かく分割','関連値を時刻付きSampleとして渡す','時刻を削除'],1,'更新単位を観測単位に合わせる。'),q('制御周期で長いロック待ちが発生。見るべきものは？',['共有範囲と処理時間、非同期境界','CPU型番だけ','出力の色'],0,'同期方法が締切を妨げていないか測定し、境界を見直す。')],
'更新途中の状態を読む失敗例と、Sampleの一括受渡し版を作って比較する。',
'位置・速度・時刻の一貫性と最大待ち時間を評価する。平均だけでなく最悪ケースも記録する。'),
deep('cs-events','中断・競合・終了処理の契約',
'commandの終了は成功だけでなく、中断、タイムアウト、無効化も含む。終了処理で機構を停止するか保持へ引き渡すかは要求によって異なる。所有権が変わる瞬間の出力も仕様にする。\n並列グループは全終了を待つ、どれか一つで終える、特定commandを期限とするなどの違いがある。残りのcommandが中断されるとき、後片付けや状態の整合が保たれるかを確認する。',
'取込とアーム移動を並列にし、取込成功でグループ終了する構成。アームが途中で中断されるなら、落下させない保持への引継ぎが必要。単にすべての出力を0にすればよいとは限らない。',
[['command終了時に常に出力0が正解とは限らない理由は？','重力機構などでは保持への制御引継ぎが必要な場合がある。機構要求に従う。'],['race型の並列実行で残りのcommandはどう検証する？','他が先に終わったときの中断・後処理・資源解放と機構の状態を試験する。']],
[q('並列の片方が先に終わりアームが中断される。確認は？',['中断時の保持・停止契約と制御権','正常終了だけ','ボタン名だけ'],0,'中間状態の機構をどう引き継ぐかを定める。'),q('2commandが同じ機構を要求する。設計するものは？',['出力を無条件合算','実行順に任せる','所有権と中断方針'],2,'競合はschedulerと機構の契約で明示的に扱う。')],
'正常終了・タイムアウト・別commandによる中断・無効化を同じ機構で再現する。',
'各終了理由での出力と状態を表にし、引継ぎ後に二重制御されないことをログで示す。'),
deep('cs-realtime','締切・鮮度・過負荷時の動作',
'リアルタイム処理では計算が正しくても締切後なら役立たない場合がある。周期、処理時間、データ年齢を別に測り、最大値と上位分位点を記録する。\n過負荷時にすべての古いジョブを処理し続けると、待ち行列が増えてさらに遅れる。用途によって最新1件を優先する、古い処理を破棄する、低頻度へ切り替えるなどを選ぶ。停止指令など捨ててはいけない種類とは分ける。',
'画像が30 fpsで来るのに推論が1枚100 msなら、毎秒20枚ずつ未処理が増える。すべて順に処理すると結果はどんどん古くなる。最新画像だけを保持する設計なら欠落は増えるが鮮度を維持しやすい。',
[['キューが増え続けると遅延が悪化する理由は？','到着率が処理率を超え、後続のデータが前の処理完了を待つため。'],['最新1件だけ残す設計が適さない例は？','必ず処理すべき停止イベントや会計的な記録など。データの意味で破棄方針を分ける。']],
[q('カメラが処理能力より速く到着し結果が古くなる。検討は？',['無制限キュー','用途に応じた最新優先と欠落記録','時刻の削除'],1,'必要な鮮度と欠落許容を仕様にする。'),q('平均処理10 ms、最大40 ms、締切20 ms。評価は？',['平均が短いので合格','締切超過があり対策と頻度評価が必要','最大を無視'],1,'平均性能だけでは締切を保証できない。')],
'到着周期33 ms、処理時間100 msのジョブ列を再現し、FIFOと最新優先のデータ年齢を比較する。',
'破棄数・最大年齢・処理件数を並べる。欠落が許される観測と、処理が必須の制御イベントを区別する。'),
];
