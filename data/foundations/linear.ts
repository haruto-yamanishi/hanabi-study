import { topic as t, f, q } from './types';
export const linearTopics = [
t('vectors','linear','ベクトルと線形結合',['functions','pythagoras'],
"ベクトルは大きさと向きを成分で表す。和と定数倍は成分ごとに計算し、\\(au+bv\\)を線形結合と呼ぶ。基底は空間の全ベクトルを一意に表せる独立な組で、成分は基底を決めて初めて意味を持つ。",
'異なる座標系の成分をそのまま加えない。','平面で平行な2本のベクトルを基底にできない理由は？','線形結合しても同じ直線上にしか届かず、平面全体を表せないから。',[
f('線形結合の成分',i=>{const a=i+2;return q(`\\(u=(${a},2)\\)、\\(v=(3,-1)\\)。\\(2u-v\\)のx成分は？`,2*a-3,`x成分に同じ演算を行い\\(2\\times ${a}-3\\)。`)}),
f('基底での表現',i=>{const a=i+2;return q(`基底\\(u=(2,0)\\)、\\(v=(0,3)\\)。\\((${2*a},6)=su+tv\\)のsは？`,a,`x成分より\\(2s=${2*a}\\)。y成分は\\(t=2\\)。`)})]),
t('dot','linear','内積と正射影',['vectors','trig'],
"内積\\(u\\cdot v\\)は対応成分の積の和で、\\(|u||v|\\cos\\theta\\)にも等しい。単位方向eへの符号付き成分は\\(u\\cdot e\\)。射影ベクトルは\\((u\\cdot e)e\\)となる。内積0は非零ベクトル同士が直交する条件。",
'方向ベクトルが単位でなければ、内積そのものは射影の長さではない。','零ベクトルとの内積0から角度90°と言えない理由は？',"零ベクトルに向きがなく、\\(\\cos \\theta\\)の式で長さ0による除算ができないため。",[
f('成分で内積',i=>{const a=i+2;return q(`\\((${a},2)\\cdot (3,-4)\\) は？`,3*a-8,`対応する成分の積を足す。\\(${a}\\times 3+2\\times (-4)\\)。`)}),
f('射影の係数',i=>{const a=i+2;return q(`\\(u=(${a},${a})\\)を\\(v=(1,2)\\)へ射影しkvとする。kは？`,3*a/5,`\\(k=\\frac{u\\cdot v}{v\\cdot v}=\\frac{${3*a}}5\\)。`)})]),
t('matrix','linear','行列積と線形写像',['vectors','simultaneous'],
'行列Aは入力ベクトルを出力へ写す。Axの各成分はAの行とxの内積。ABは先にB、次にAを作用させる合成で、列数と行数が合う必要がある。通常ABとBAは等しくない。',
'行列積は同じ位置の要素同士を掛ける演算ではない。','行列積の順序が重要な理由を回転と平行方向の拡大で説明する。','先にどの軸へ拡大するかが変わるため、回転後の結果が一般に異なる。',[
f('行列とベクトル',i=>{const a=i+2;return q(`\\(A=\\begin{bmatrix}2&1\\\\0&3\\end{bmatrix}\\)、\\(x=(${a},4)\\)。Axの第1成分は？`,2*a+4,`第1行とxの内積\\(2\\times ${a}+1\\times 4\\)。`)}),
f('行列合成の要素',i=>{const a=i+2;return q(`\\(A=\\begin{bmatrix}1&${a}\\\\0&1\\end{bmatrix}\\)、\\(B=\\begin{bmatrix}2&0\\\\3&1\\end{bmatrix}\\)。ABの左上要素は？`,2+3*a,`Aの第1行とBの第1列を掛けて足す。\\(1\\times 2+${a}\\times 3\\)。`)})]),
t('det','linear','行列式と面積倍率',['matrix'],
"\\(2\\times 2\\)行列\\(\\begin{bmatrix}a&b\\\\c&d\\end{bmatrix}\\)の行列式は\\(ad-bc\\)。単位正方形が写された平行四辺形の符号付き面積を表す。0なら平面が直線以下へ潰れ、逆写像を作れない。面積倍率は絶対値。",
'負の行列式は面積が負という意味ではなく向きの反転を含む。','行列式0と連立方程式の一意解の関係を説明する。','列が独立でなく入力情報が失われるため、一意に逆算できない。右辺次第で解なしまたは無数。',[
f('2次行列式',i=>{const a=i+2;return q(`\\(\\begin{bmatrix}${a}&2\\\\3&4\\end{bmatrix}\\)の行列式は？`,4*a-6,`\\(ad-bc\\)を計算する。\\(${a}\\times 4-2\\times 3\\)。`)}),
f('面積倍率',i=>{const a=i+2;return q(`変換行列\\(\\operatorname{diag}(-${a},2)\\)で面積3の図形を写す。面積は？`,6*a,`行列式\\(-${2*a}\\)の絶対値を元の面積3に掛ける。`)})]),
t('inverse','linear','逆行列と逆変換',['det'],
"\\(A^{-1}A=I\\)を満たす行列が逆行列。\\(2\\times 2\\)では行列式が0でなければ\\(A^{-1}=\\frac1{\\det A}\\begin{bmatrix}d&-b\\\\-c&a\\end{bmatrix}\\)。数値計算では逆行列を明示的に作るより、連立方程式を解く方法を選ぶことも多い。",
'各要素の逆数を並べても逆行列にはならない。','逆行列を求めたあと、どの積で確認する？',"\\(A^{-1}A\\)と\\(AA^{-1}\\)が単位行列になることを確かめる。",[
f('対角行列の逆',i=>{const a=i+2;return q(`\\(\\operatorname{diag}(${a},3)\\)の逆行列の左上要素は？`,1/a,`同じ成分の積が1となるよう\\(\\frac{1}{${a}}\\)。`)}),
f('せん断を戻す',i=>{const a=i+2;return q(`\\(A=\\begin{bmatrix}1&${a}\\\\0&1\\end{bmatrix}\\)、\\(Ax=(${2*a+3},2)\\)。xの第1成分は？`,3,`第2成分2を使い\\(x_{1}+${a}\\times 2=${2*a+3}\\)を解く。`)})]),
t('rank','linear','階数・基底・核',['matrix','simultaneous'],
"階数は独立な列の最大数。n列の行列では階数と核の次元の和がnになる。核は\\(Ax=0\\)となる入力の集合で、観測から区別できない方向に対応する。行基本変形で独立な条件を数えられる。",
'非零要素の数や行数だけでは階数は決まらない。','観測行列の核にある変化をセンサで区別できない理由は？',"\\(A\\delta x=0\\)なので、xと\\(x+\\delta x\\)の出力が同じになるため。",[
f('比例する行',i=>{const a=i+2;return q(`\\(A=\\begin{bmatrix}1&${a}\\\\2&${2*a}\\end{bmatrix}\\)の階数は？`,1,`第2行は第1行の2倍。非零の独立な行は1本。`)}),
f('核の次元',i=>{const n=i+3;return q(`${n}列、階数2の行列。核の次元は？`,n-2,`階数・退化次数定理で\\(${n}-2\\)。`)})]),
t('eigen','linear','固有値と反復変換',['det','inverse'],
"\\(Av=\\lambda v\\)を満たす非零vを固有ベクトル、\\(\\lambda\\)を固有値という。方向を保ったまま倍率だけが変わる。三角行列の固有値は対角成分。離散時間の反復では\\(|\\lambda|<1\\)のモードは減衰する。",
'連続時間と離散時間の安定条件を混同しない。固有ベクトルは零であってはならない。','固有ベクトル方向でAを2回作用させると倍率は？',"\\(A^{2}v=A(\\lambda v)=\\lambda Av=\\lambda ^{2}v\\)なので\\(\\lambda ^{2}\\)。",[
f('固有方向の倍率',i=>{const a=i+2;return q(`\\(A=\\operatorname{diag}(${a},2)\\)、\\(v=(1,0)\\)。\\(Av=\\lambda v\\)の\\(\\lambda\\)は？`,a,`第1軸方向は${a}倍される。`)}),
f('2回の作用',i=>{const a=i+2;return q(`\\(Av=${a}v\\)、\\(v\\ne 0\\)。\\(A^{2}v=kv\\)のkは？`,a*a,`線形性より\\(A(${a}v)=${a}Av=${a*a}v\\)。`)})]),
t('least-squares','linear','最小二乗と残差',['dot','matrix','det'],
"過剰な観測\\(Ax\\approx b\\)には厳密解がないことがある。残差\\(r=b-Ax\\)の二乗和を最小にするのが最小二乗。列が独立なら正規方程式\\(A^\\mathsf{T}Ax=A^\\mathsf{T}b\\)で一意解を得る。実装では数値的に安定なQR等も使う。",
'残差が小さくても、モデルの仮定や外挿の正しさは保証しない。','最小二乗で二乗を使うと正負の残差は打ち消し合う？','打ち消さない。各残差の二乗は非負で、大きな誤差を強く罰する。',[
f('定数モデルの最適値',i=>{const a=i+2;return q(`測定${a},${a+2},${a+7}へ定数cを最小二乗で当てる。cは？`,a+3,`定数モデルの最適値は平均。合計\\(\\frac{${3*a+9}}{3}\\)。`)}),
f('原点を通る直線',i=>{const a=i+2;return q(`点\\((1,${a}),(2,${2*a+5})\\)へ\\(y=kx\\)を最小二乗で当てる。kは？`,a+2,`\\(k=\\frac{\\sum xy}{\\sum x^{2}}=\\frac{(${a}+2\\times ${2*a+5})}{5}\\)。`)})]),
];
