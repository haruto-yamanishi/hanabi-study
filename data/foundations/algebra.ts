import { topic as t, f, q } from './types';
export const algebraTopics = [
t('expand','algebra','分配法則と多項式',['signed','powers'],
'分配法則a(b+c)=ab+acで括弧を外す。同類項は文字と指数が同じ項だけをまとめる。二項式の積は各項を一度ずつ掛け、(x+a)(x+b)=x²+(a+b)x+abとなる。',
'−(x−a)の括弧を外すと−x+a。負号は全項に分配する。','(x+a)²の中央の項が2axになる理由は？','(x+a)(x+a)の交差する積axが2つ出るため。',[
f('展開の係数',i=>{const a=i+2,b=i%7+1;return q(`(x+${a})(x+${b})を展開したxの係数は？`,a+b,`x²+(${a}+${b})x+${a*b}。`)}),
f('負号を含む整理',i=>{const a=i+2;return q(`${a}(x−3)−2(x+1) の定数項は？`,-3*a-2,`${a}x−${3*a}−2x−2より定数項を加算。`)})]),
t('factor','algebra','因数分解と零積',['expand'],
'因数分解は和を積へ戻す操作。共通因数をくくり、次に公式を調べる。\\(x^2-a^2=(x-a)(x+a)\\)。実数の積uv=0ならu=0またはv=0となり、方程式を小さな問題へ分けられる。',
'和u+v=0からu=0またはv=0とはいえない。','因数分解した結果が正しいか確かめる方法は？','積を展開して元の式の各係数と比較する。数値の一点代入だけでは恒等式の証明にはならない。',[
f('平方差',i=>{const a=i+2;return q(`x²−${a*a}=(x−a)(x+a)と書く。a>0のaは？`,a,`定数項が\\(-a^2\\)なのでa=√${a*a}。`)}),
f('零積から解',i=>{const a=i+2;return q(`(x−${a})(x+3)=0 の正の解は？`,a,`各因数を0とおくとx=${a}または−3。`)})]),
t('linear-eq','algebra','一次方程式と式の逆算',['fractions','expand'],
'等式の両辺へ同じ操作を行えば等式は保たれる。ただし0で割らない。ax+b=cで\\(a\\ne0\\)なら、bを引いてaで割りx=(c−b)/a。求めた値は元の式へ戻し検算する。',
'移項は符号を機械的に変える規則ではなく、両辺へ同じ数を加減する操作。','ax+b=cでa=0のとき何が起こる？','b=cなら全てのx、b≠cなら解なし。aで割る方法は使えない。',[
f('一次方程式',i=>{const a=i%5+2,x=i+1;return q(`${a}x+7=${a*x+7} を解く。`,x,`両辺から7を引き${a}で割る。(${a*x+7}−7)/${a}=${x}。`)}),
f('未知数が両辺',i=>{const a=i+3;return q(`3x+${a}=2x+${2*a} を解く。`,a,`両辺から2xと${a}を引き、x=${a}。`)})]),
t('simultaneous','algebra','連立方程式と消去',['linear-eq'],
'未知数が2つなら独立な条件が2つ必要になる。係数を揃えて加減し、一方を消去して残りを求める。同じ条件を繰り返しても情報は増えず、矛盾する条件には解がない。',
'片方の式だけを満たす値では不十分。両方に代入する。','2本の式が同じ直線を表すと解が一意に定まらない理由は？','交点が1点ではなく直線全体になり、独立な制約が1本分しかないから。',[
f('加減法',i=>{const x=i+2,y=i%6+1;return q(`x+y=${x+y}、x−y=${x-y}。xは？`,x,`式を加えると2x=${2*x}。`)}),
f('代入による確認',i=>{const x=i+2,y=i%5+2;return q(`2x+y=${2*x+y}、x+2y=${x+2*y}。yは？`,y,`第2式を2倍し第1式を引くと3y=${3*y}。`)})]),
t('quadratic','algebra','二次方程式と判別式',['factor','linear-eq','roots'],
'ax²+bx+c=0、\\(a\\ne0\\)の解は(−b±√(b²−4ac))/(2a)。平方完成から導ける。判別式D=b²−4acが正なら異なる実数解2つ、0なら重解、負なら実数解はない。物理では定義域も確認する。',
'二乗して得た解は元の式を満たさない場合がある。','実数解が2つあっても物理で片方を除く場合を挙げる。','開始後の到達時刻で負の時間を除くなど、モデルの定義域で選ぶ。',[
f('根と係数',i=>{const a=i+2;return q(`x²−${a+1}x+${a}=0 の大きい解は？`,a,`(x−1)(x−${a})=0。${a}>1。`)}),
f('判別式',i=>{const a=i+2;return q(`x²+${a}x−3=0 の判別式Dは？`,a*a+12,`b²−4ac=${a}²−4×1×(−3)。`)})]),
t('inequality','algebra','不等式と解の範囲',['linear-eq'],
'不等式も両辺に同じ数を加減できる。正数を掛けても向きは変わらないが、負数で乗除すると向きが反転する。解は単一の数ではなく範囲になり、境界を含むかも区別する。',
'−2x<6をx<−3としない。負数で割るためx>−3。','負数を掛けると不等号が反転する理由を数直線で説明する。','原点について反転するので、左右の順序が入れ替わる。',[
f('負数で割る境界',i=>{const a=i+2;return q(`−2x<${2*a} の解はx>B。Bは？`,-a,`−2で割ると向きが反転してx>−${a}。`)}),
f('整数の最大値',i=>{const a=i+2;return q(`3x+1<${3*a+1} を満たす整数xの最大値は？`,a-1,`x<${a}なので最大整数は${a-1}。`)})]),
t('functions','algebra','関数・合成・逆関数',['linear-eq'],
'関数は入力ごとに出力が1つ定まる対応。f(g(x))は先にg、その出力へfを作用させる。逆関数は出力から入力を一意に戻せる範囲で定義する。式だけでなく定義域と値域が必要。',
'f(g(x))とg(f(x))は一般に異なる。二乗の逆は定義域を制限しないと一意にならない。','x²の逆関数を作るとき定義域を制限する理由は？','xと−xが同じ値へ写るため、そのままでは入力を一意に戻せない。',[
f('合成関数',i=>{const a=i+2;return q(`f(x)=2x+3、g(x)=x−4。f(g(${a}))は？`,2*(a-4)+3,`g(${a})=${a-4}をfへ代入。`)}),
f('逆関数',i=>{const a=i+2;return q(`f(x)=3x−2でf(x)=${3*a-2}。入力xは？`,a,`出力に2を足して3で割る。`)})]),
t('exp-log','algebra','指数関数と対数',['powers','functions'],
'a>0、a≠1に対しlogₐxはaの何乗がxかを表し、x>0で定義する。logₐ(xy)=logₐx+logₐyは指数法則の逆。指数関数は等しい時間に同じ倍率で変わる現象へ使う。',
'log(x+y)をlogx+logyに分けない。対数の底と真数の条件を忘れない。','指数的増加と一定量ずつの増加は何が違う？','前者は比が一定、後者は差が一定。グラフの傾きも異なる。',[
f('対数の定義',i=>{const a=i+2;return q(`log₂(2^${a}) は？`,a,`2を${a}乗した値なので、対数は指数${a}を返す。`)}),
f('指数の逆算',i=>{const a=i+2;return q(`初期量3が毎期2倍。${a}期後は3×2^kと表せる。kは？`,a,`倍率2を${a}回掛けるため指数は${a}。初期量は指数に含めない。`)})]),
t('pythagoras','geometry','直角三角形と距離',['roots'],
'直角三角形の斜辺cと他の辺a,bには\\(c^2=a^2+b^2\\)が成り立つ。座標間の距離にも、差の成分を直角三角形の辺とみて使える。直角でない三角形にそのまま使わない。',
'距離は成分の単純な和ではない。','座標間距離を求めるとき座標そのものではなく差を使う理由は？','原点の移動で距離が変わらないよう、2点の相対的な位置を使うため。',[
f('斜辺',i=>{const a=i+1;return q(`直角三角形の2辺が${3*a}と${4*a}。斜辺は？`,5*a,`√(${3*a}²+${4*a}²)=√${25*a*a}=${5*a}。`)}),
f('2点の距離',i=>{const a=i+1;return q(`点(2,3)と(${2+3*a},${3+4*a})の距離は？`,5*a,`差は(${3*a},${4*a})。距離は差ベクトルの長さ。`)})]),
t('similarity','geometry','相似と面積・体積の倍率',['ratio','powers'],
'相似な図形では対応する長さが同じ倍率kになる。面積は独立な長さ2方向の積なのでk²、体積は3方向でk³となる。質量の倍率には密度も同じという条件が必要。',
'長さ2倍なら面積4倍、体積8倍。すべて2倍ではない。','模型を大型化すると質量と断面積が同じ倍率で増えない理由は？','体積は3次元、断面積は2次元の量だから。',[
f('面積の倍率',i=>{const a=i+2;return q(`相似比が${a}:1。大きい図形の面積は小さい図形の何倍？`,a*a,`長さの倍率を二乗し${a}²。`)}),
f('体積から長さ',i=>{const a=i+2;return q(`相似な立体の体積比が${a*a*a}:1。長さの比の大きい側は？`,a,`体積倍率の立方根を取る。${a}³=${a*a*a}。`)})]),
t('trig','geometry','三角比と成分分解',['pythagoras','ratio'],
'直角三角形でsinθ=対辺/斜辺、cosθ=隣辺/斜辺、tanθ=対辺/隣辺。向きを含むベクトルではx=\\(r\\cos\\theta\\)、y=\\(r\\sin\\theta\\)と単位円へ拡張する。θの基準軸を図に書く。',
'角度が鉛直からか水平からかでsinとcosの役割が変わる。','同じ力の成分分解でsinとcosが入れ替わることがある理由は？','角度を測る基準軸が変わるため。図で対辺と隣辺を確認する。',[
f('30度の成分',i=>{const a=i+2;return q(`大きさ${2*a} N、水平から30°の力。鉛直成分は？`,a,`Fy=F sin30°=${2*a}×\\(\\frac12\\)。`,'N')}),
f('三角比から辺',i=>{const a=i+2;return q(`直角三角形でsinθ=3/5、対辺が${3*a}。斜辺は？`,5*a,`対辺/斜辺=3/5なので斜辺=${3*a}×5/3。`)})]),
t('radians','geometry','ラジアンと周期',['trig','units'],
'角度θを弧長s/半径rで定義したものがラジアン。1周は2π rad=360°。回転数f[Hz]から角速度はω=2πf、周期は1/f。微分の三角関数公式はラジアンを前提とする。',
'度をそのままsinの微分公式や\\(v=r\\omega\\)へ入れない。','ラジアンが長さの単位を持たないのに角度を表せる理由は？','弧長と半径の比であり、長さ同士の単位が相殺されるから。',[
f('弧長から角度',i=>{const a=i+2;return q(`半径2 m、弧長${a} m。中心角は？`,a/2,`θ=s/r=${a}/2。`,'rad')}),
f('回転数から周期',i=>{const a=i+2;return q(`毎秒${a}回転する周期は？`,1/a,`T=1/f=1/${a}。`,'s')})]),
];
