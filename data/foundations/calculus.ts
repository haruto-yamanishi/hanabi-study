import { topic as t, f, q } from './types';
export const calculusTopics = [
t('limits','calculus','極限と連続性',['functions','factor'],
'xがaに限りなく近づくときf(x)が近づく値を極限という。点aでの値そのものとは区別し、左右から同じ値に近づく必要がある。極限とf(a)が一致すればそこで連続。0/0は値ではなく、式の変形を要する不定形。',
'分母0へそのまま代入して極限なしと結論しない。約分はx≠aの範囲で行う。','点で関数が未定義でも極限が存在する例を示す。','(x²−1)/(x−1)はx=1で未定義だが、近傍でx+1に等しく極限2。',[
f('約分して極限',i=>{const a=i+2;return q(`x→${a}で(x²−${a*a})/(x−${a})の極限は？`,2*a,`x≠${a}でx+${a}へ約分できるので極限${2*a}。`)}),
f('連続になる補完',i=>{const a=i+2;return q(`x≠0でf(x)=((${a}+x)²−${a*a})/x。連続にするf(0)は？`,2*a,`展開すると2×${a}+x。x→0の極限を点の値にする。`)})]),
t('derivative','calculus','微分係数と導関数',['limits'],
'微分係数f′(a)は差商(f(a+h)−f(a))/hのh→0での極限。局所的な傾きや瞬間変化率を表す。xⁿの導関数はnxⁿ⁻¹、定数の導関数は0。導関数の単位は出力の単位/入力の単位。',
'平均変化率と瞬間変化率を混同しない。微分可能なら連続だが逆は一般に偽。','|x|が0で微分できない理由は？','左の傾き−1と右の傾き1が一致しないから。',[
f('多項式の微分',i=>{const a=i+2;return q(`f(x)=${a}x²+3x。f′(2)は？`,4*a+3,`f′(x)=${2*a}x+3へx=2を代入。`)}),
f('平均変化率',i=>{const a=i+2;return q(`f(x)=x²、区間[${a},${a+1}]の平均変化率は？`,2*a+1,`((${a+1})²−${a}²)/((${a+1})−${a})=${2*a+1}。`)})]),
t('chain','calculus','積の微分と連鎖律',['derivative','expand'],
'積の微分は(fg)′=f′g+fg′。合成は(f(g(x)))′=f′(g(x))g′(x)となる。外側と内側の変化率を分け、依存関係を矢印で書くと抜けを防げる。',
'(fg)′をf′g′としない。合成の内側の微分を忘れない。','単位の観点から連鎖律を説明する。','dy/duとdu/dxを掛けると中間量uの単位が消え、dy/dxになる。',[
f('合成の微分',i=>{const a=i+2;return q(`f(x)=(${a}x+1)²。f′(1)は？`,2*a*(a+1),`外側2(${a}x+1)に内側の微分${a}を掛ける。`)}),
f('積の微分',i=>{const a=i+2;return q(`f(x)=x²(x+${a})。f′(1)は？`,2*a+3,`2x(x+${a})+x²へ1を代入して2(1+${a})+1。`)})]),
t('extrema','calculus','増減・極値・最適化',['derivative','quadratic'],
'f′の符号で増減を判定する。内点の微分可能な極値では\\(f^{\\prime}=0\\)が必要だが十分ではない。閉区間の最大最小は停留点だけでなく端点も比較する。\\(f^{\\prime\\prime}>0\\)なら下に凸で局所最小を判定できる。',
'\\(f^{\\prime}=0\\)だから必ず極値とはいえない。x³の0が反例。','制約区間で端点を調べる必要がある理由は？','区間内で単調でも最大最小は端に存在するため。端では両方向の微分条件を要求できない。',[
f('二次関数の最小位置',i=>{const a=i+2;return q(`\\(f(x)=x^2-${2*a}x+7\\)。最小となるxは？`,a,`\\(f^{\\prime}=2x-${2*a}=0\\)、\\(f^{\\prime\\prime}=2>0\\)。`)}),
f('区間の端点',i=>{const a=i+2;return q(`\\(f(x)=(x-${a+2})^2\\)、0≤x≤${a}。最小値は？`,4,`頂点は区間外。区間内で減少し、右端x=${a}で(−2)²=4。`)})]),
t('integral','calculus','原始関数と定積分',['derivative'],
'原始関数FはF′=fを満たす。xⁿの原始関数は\\(n\\ne-1\\)で\\(\\frac{x^{n+1}}{n+1}+C\\)。定積分は符号付きの累積量で、微積分の基本定理により次のように計算できる。\\[\\int_a^b f(x)\\,dx=F(b)-F(a)\\]',
'不定積分の積分定数と、定積分の上下限を忘れない。','速度の定積分と総移動距離は常に同じ？','速度の積分は変位。逆向き区間も正に数える総距離は速さ|v|を積分する。',[
f('多項式の定積分',i=>{const a=i+2;return q(`\\(\\int_0^2 ${a}x\\,dx\\) は？`,2*a,`原始関数${a}x²/2を上下限へ代入。`)}),
f('累積量',i=>{const a=i+2;return q(`速度\\(v(t)=${a}t^2\\) m/s。t=0から3秒の変位は？`,9*a,`\\(\\int_0^3 ${a}t^2\\,dt=\\left[\\frac{${a}t^3}{3}\\right]_0^3=${9*a}\\)。`,'m')})]),
t('ftc','calculus','積分で定義された関数',['integral','chain'],
'\\(F(x)=\\int_a^x f(t)\\,dt\\)なら、fが連続な点で\\(F^{\\prime}(x)=f(x)\\)。上端がg(x)なら連鎖律によりf(g(x))g′(x)。上下限が両方動く場合は上端側から下端側の寄与を引く。',
'積分変数tと上端xを区別する。上端の微分を落とさない。','累積量の微分が元の変化率になる直感を説明する。','上端を少し動かしたとき増える細い区間の量が、幅×その点の変化率に近いから。',[
f('基本定理',i=>{const a=i+2;return q(`\\(F(x)=\\int_0^x (t^2+${a})\\,dt\\)。\\(F^{\\prime}(2)\\)は？`,a+4,`上端の被積分関数2²+${a}。`)}),
f('動く上端',i=>{const a=i+2;return q(`\\(F(x)=\\int_0^{x^2} ${a}t\\,dt\\)。\\(F^{\\prime}(2)\\)は？`,16*a,`被積分関数${a}x²に上端の微分2xを掛け、x=2。`)})]),
t('partial','calculus','偏微分・勾配・方向微分',['chain','dot'],
'多変数関数の偏微分は、他の変数を固定して1変数だけを動かす。勾配\\(\\nabla f\\)は偏微分を並べたベクトル。単位方向uへの変化率は\\(\\nabla f\\cdot u\\)。依存する変数を固定扱いにしないよう注意する。',
'偏微分と全微分を混同しない。方向ベクトルは正規化する。','温度分布の勾配が示す方向は？','局所的に最も温度が増加する方向。勾配の大きさはその最大変化率。',[
f('偏微分',i=>{const a=i+2;return q(`\\(f(x,y)=${a}x^2y+y^2\\)。(1,2)で\\(\\frac{\\partial f}{\\partial x}\\)は？`,4*a,`yを固定して2×${a}xyへ(1,2)を代入。`)}),
f('方向微分',i=>{const a=i+2;return q(`ある点の勾配が(${a},2)、単位方向u=(3/5,4/5)。方向微分は？`,(3*a+8)/5,`勾配と単位方向の内積${a}×3/5+2×4/5。`)})]),
t('double-integral','calculus','二重積分と領域',['partial','integral'],
'二重積分は平面の小さな面積ごとに量を足す操作。長方形領域なら一方の変数を固定して積分し、次にもう一方を積分できる。変数に依存する境界では、内側と外側の積分区間を図から決める。',
'三角形の領域を外接長方形として積分しない。','面密度の二重積分が質量になることを単位で説明する。','kg/m²に微小面積m²を掛け、領域全体で加算するためkgになる。',[
f('長方形領域',i=>{const a=i+2;return q(`0≤x≤2、0≤y≤3でf(x,y)=${a}x。二重積分の値は？`,6*a,`xについて\\(\\int_0^2 ${a}x\\,dx\\)=${2*a}、y方向の長さ3を掛ける。`)}),
f('三角形領域の面積',i=>{const a=i+2;return q(`0≤x≤${a}、0≤y≤xの領域で1を二重積分した値は？`,a*a/2,`内側∫₀ˣ1dy=x、外側∫₀^${a}x dx=${a*a}/2。`)})]),
t('sequence','analysis','数列・極限・誤差',['limits','inequality'],
'数列は自然数nを入力とする関数。収束とは任意に小さい許容幅に対し、十分後の全項が極限の近くに入ること。最初の数項が近いだけでは不十分。誤差の上界が分かれば必要な項数を見積もれる。',
'有限個の計算結果だけで収束を証明しない。','「十分大きいn以降の全項」という条件が必要な理由は？','何度も遠くへ飛ぶ数列を、たまたま近い項だけで収束と誤判定しないため。',[
f('誤差の上界',i=>{const a=i+2;return q(`aₙ=3+${a}/n。n=${10*a}で極限3からの絶対誤差は？`,0.1,`|aₙ−3|=${a}/${10*a}=0.1。`)}),
f('必要な項数',i=>{const a=i+2;return q(`aₙ=2+${a}/n。誤差を0.01以下にする最小正整数nは？`,100*a,`${a}/n≤0.01よりn≥${100*a}。`)})]),
t('series','analysis','等比級数と打切り誤差',['sequence','powers'],
'初項a、公比rの等比数列のn項和はr≠1でa(1−rⁿ)/(1−r)。|r|<1なら無限和a/(1−r)へ収束し、n項で止めた残差はarⁿ/(1−r)。項が0へ近づくことは一般の級数の収束の十分条件ではない。',
'Σ1/nは各項が0へ近づくが発散する。等比級数の条件を他の級数へ流用しない。','等比級数の和の式をS−rSから説明する。','各項が相殺し(1−r)S=a(1−rⁿ)が残るため。',[
f('無限等比和',i=>{const a=i+2;return q(`初項${a}、公比\\(\\frac12\\)の無限等比級数の和は？`,2*a,`${a}/(1−\\(\\frac12\\))=${2*a}。`)}),
f('打切り残差',i=>{const a=i+2;return q(`初項${a}、公比\\(\\frac12\\)。最初の4項で止めた残りの和は？`,a/8,`${a}×(\\(\\frac12\\))⁴/(1−\\(\\frac12\\))=${a}/8。`)})]),
t('epsilon','analysis','εとδによる近さの保証',['limits','inequality'],
'limₓ→ₐf(x)=Lは、任意のε>0に対しδ>0を選び、0<|x−a|<δなら|f(x)−L|<εを保証できること。線形関数kx+bでは出力誤差が|k|倍なのでδ=ε/|k|を使える。',
'δはεに応じて選べるが、後から選ぶ個別のxには依存させない。','εとδの役割の違いを説明する。','εは求められた出力誤差、δはそれを保証する入力側の近さ。',[
f('線形関数のδ',i=>{const a=i+2;return q(`f(x)=${a}x+1。出力誤差ε=0.1に対しδ=ε/|傾き|で選ぶδは？`,0.1/a,`|f(x)−f(c)|=${a}|x−c|なのでδ=0.1/${a}。`)}),
f('入力誤差から出力誤差',i=>{const a=i+2;return q(`f(x)=−${a}x+2、|x−c|≤0.02。出力誤差の上界は？`,a*0.02,`傾きの絶対値${a}に入力誤差0.02を掛ける。`)})]),
t('taylor','analysis','テイラー近似と残差',['chain','series'],
'滑らかな関数は基準点近くでf(a)+f′(a)h+f″(a)h²/2+…と近似できる。一次近似には二次以上の残差がある。使う範囲で高階微分の上界を調べると、残差の保証に結びつく。',
'基準点から遠くでも低次近似が正確とは限らない。','近似の次数を上げることと使用範囲を広げることは同じ？','同じではない。収束半径や残差の評価が別に必要。',[
f('平方根の一次近似',i=>{const h=(i+1)/1000;return q(`√(1+h)≈1+h/2を使い、h=${h}での近似値は？`,1+h/2,`f(1)=1、f′(1)=\\(\\frac12\\)なので1+${h}/2。`)}),
f('二乗関数の残差',i=>{const h=(i+1)/100;return q(`(1+h)²を1+2hで近似する。h=${h}での厳密な残差は？`,h*h,`展開の残りh²=${h}²。近似値ではなく差を求める。`)})]),
t('ode','analysis','微分方程式と初期条件',['integral','exp-log'],
'微分方程式は未知の関数と導関数の関係。y′=kyの解はy=C e^(kt)で、初期値y(0)=y₀からC=y₀が定まる。解を元の式へ代入し、微分方程式と初期条件を両方満たすか確認する。',
'微分方程式だけでは積分定数が残る。初期条件を無視しない。','同じ微分方程式から複数の軌道が得られる理由は？','初期状態により積分定数が変わるから。',[
f('一定の変化率',i=>{const a=i+2;return q(`y′=${a}、y(0)=3。y(2)は？`,2*a+3,`積分してy=${a}t+C。C=3、t=2を代入。`)}),
f('指数解の確認',i=>{const a=i+2;return q(`y(t)=3e^(−${a}t)。y′(0)は？`,-3*a,`微分は−${a}×3e^(−${a}t)、t=0ではe⁰=1。`)})]),
t('euler','analysis','数値積分と刻み幅',['ode'],
'オイラー法はyₙ₊₁=yₙ+h f(tₙ,yₙ)で微分方程式を進める。現在の傾きを区間内で一定と近似する。刻み幅を小さくした結果や解析解と比較して誤差を確かめる。安定性にも刻み幅が影響する。',
'小さい刻みでも丸め誤差は残る。見た目が滑らかであることは正確さの保証ではない。','減衰方程式でも大きな刻みで数値解が発散する理由は？','離散化の倍率が1を超える場合があり、元の連続モデルと異なる増幅が生じるため。',[
f('1ステップ更新',i=>{const a=i+2;return q(`y′=−2y、現在y=${a}、刻みh=0.1。オイラー法の次のyは？`,0.8*a,`y+h(−2y)=${a}−0.2×${a}。`)}),
f('離散化の倍率',i=>{const a=i+2;return q(`y′=−${a}y、h=0.01。オイラー更新yₙ₊₁=ryₙのrは？`,1-a/100,`r=1−${a}×0.01。|r|<1がこの減衰問題の離散安定条件。`)})]),
];
